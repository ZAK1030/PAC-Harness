# 新建场景向导与 ToUser 网页工作台

## 启动与使用

在 Core 项目目录执行：

```powershell
python -m pac_harness --web
```

Linux 使用：

```bash
python3 -m pac_harness --web
```

复制终端输出的完整链接到浏览器。服务仅监听 `127.0.0.1`，使用随机访问令牌，并校验 Host 和 Origin。令牌保存在当前浏览器标签页的会话存储中；服务器重启后需使用新链接。端口可用 `--port 8766` 指定。不要把含令牌的链接共享给他人。

1. 点击“新建场景”，输入小写英文标识（例如 `document-review`）。
2. 描述目标、成功条件、输入、允许操作、API/SDK 接口以及失败恢复方式。不清楚的部分写“待确认”。
3. 点击“创建并开始对话”。目录和 `SCENE.md` 先保存，再自动提交首条消息。即使模型连接失败，场景仍会保留。
4. 在对话框中回答 ToUser 的问题。支持多行和 Ctrl/⌘+Enter 发送；模型处理期间禁用重复发送。长期修改必须实际编辑文件；缺少 diff、语法或离线测试失败会在同一会话自动修正（默认最多 2 次）。仍失败时会保留错误，可以补充信息继续。
5. 查看右侧修改差异和验证结果，包括 `repair-*` 中的自动修正与测试日志。复用原 ToUser 隔离副本、离线测试、并发检查和备份流程；验证通过后应用。应用成功会结束本轮协助，点击“新一轮协助”继续完善。
6. 按 ToUser 提供的配置步骤修改场景 `config.json`，完成离线测试，再执行网页右侧的预览命令。

“结束协助”会在当前请求处理完后退出，不强行终止已开始的验证或应用。它不等于恢复业务任务。

也可在对话中要求“结束本轮协助”。ToUser 的返回意图会真正结束会话；如果还有未提交的候选修改，会标明未应用。网页没有绑定运行任务，临时指导只进入对话审计；需要未来任务持续采用的事实和流程，应写入该场景的 `memories/` 或 `skills/`。

## 目录与隔离

每个新场景位于启动项目的 `scenes/<标识>/`。仅复制核心、通用示例、测试、角色提示词和文档；生成新的空白记忆、技能目录及默认配置，不继承旧业务记忆、配置、凭据或日志。角色提示词与核心代码来自当前项目，应以干净 Core 作为创建源。每场景核心副本需要显式同步升级。

`SCENE.md` 是需求说明，ToUser 应把确认的长期事实写入记忆和技能，Planner/Detector 不自动读取该文件。适配器配置初始为 `adapters.pending:create`，故意不提供可执行业务实现，直到接入完成。模型也需要配置。

聊天记录保存在各场景的 `maintenance/web/<会话ID>.json`，原 ToUser 审计位于 `maintenance/to-user-*/`。刷新页面可查看和继续当前活跃会话；服务器重启后旧活跃会话标记为中断，可以查看历史并开始新一轮。新一轮会读取已保存的项目知识，但不自动向模型回放旧会话。

同一场景只允许一个活跃网页协助会话。网页协助与新版 CLI 共享 `maintenance/web-active/` 下的进程锁，避免运行期间修改代码；旧版本 CLI、直接调用 Harness 的程序和外部编辑器不受这个入口锁控制。不同场景的目录独立，但 Python 依赖、账号和设备不自动隔离。

## 当前范围

- 已实现：场景创建、描述自动提交、多轮文本对话、状态展示、历史保存、修改差异与验证查看、目录与入口互斥。
- 仍使用现有本机 Codex CLI 和模型连接设置；工作台不安装 CLI 或管理账号。新场景不会复制项目的 `to_user` 定制参数，需要时在新场景配置中填写。
- 本版是项目协助，不绑定 `task_state.json`，不提供业务启动、运行中暂停或恢复按钮。业务执行继续使用场景内 CLI。
- 暂未提供附件上传、Markdown 富文本渲染或 Git 自动提交。输出使用安全纯文本展示。Git 仍建议按场景单独管理。
- 修改核心 Python 后须重启工作台才能加载新版本。结束服务前应先结束各活跃会话并等待完成，避免中断正在进行的修改应用。

## 验证

运行 `python -m unittest discover -s tests -v`。新增测试覆盖场景创建和覆盖拒绝、路径限制、敏感旧配置与记忆不继承、多轮会话持久化、场景间记录隔离、项目锁与 HTTP 访问校验。真实 Codex 的端到端连接需要本机模型环境可用。
