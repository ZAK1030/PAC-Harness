# ToUser 网页工作台

## 启动与使用

在当前项目目录执行（Linux 可使用 `python3`）：

```bash
python -m pac_harness --web
```

打开终端输出的完整本机链接，保留 `#token=...`。服务只监听 `127.0.0.1`，API 校验令牌、Host 和 Origin；不要分享访问令牌。端口冲突时加 `--port 8766`。

网页显示当前项目路径，直接输入问题或修改需求即可开始对话。可要求 ToUser 分析问题、修改代码、把确认事实写入 `memories/`，或在 `skills/` 中建立可复用流程。无需创建或选择场景。

支持多行输入、草稿保存、连续对话、会话历史、修改差异和验证结果。输入期间可使用页面的发送与结束按钮。结束请求等待当前推理或验证完成，不会强制中断修改应用。

## 会话与修改

聊天记录保存在当前项目 `maintenance/web/<会话ID>.json`，ToUser 审计位于 `maintenance/to-user-*/`。刷新页面可继续当前活跃会话；服务器重启后旧活跃会话标记为中断，可查看历史并开始新对话。新对话读取已保存的项目知识，但不自动向模型回放旧会话。

同一项目只允许一个活跃网页协助会话。网页与 CLI 共用 `maintenance/web-active/` 进程锁，避免运行任务时修改代码；直接调用 Harness 的程序和外部编辑器不受该入口锁控制。

ToUser 保留暂存副本、离线验证和应用前校验，通过验证后才应用修改。纯讨论不会自动保存为长期知识，持久知识需要实际写入记忆或技能文件。

## 当前范围

- 使用本机 Codex CLI 和项目 `config.json` 的 `to_user` 配置；工作台不安装 CLI 或管理账号。
- 网页用于项目协助，不绑定运行中的业务任务，不提供业务启动、暂停或恢复按钮。结束会话后在终端运行任务。
- 暂无附件上传、Markdown 富文本渲染或 Git 自动提交；消息以纯文本显示。
- 修改 Python 代码后需要重启工作台加载新版本。停止服务前先结束活跃会话并等待完成。

## 验证

运行 `python -m unittest discover -s tests -v`。离线测试覆盖多轮会话持久化、项目互斥、修改应用与自动修正审计、HTTP 访问校验及旧创建入口移除。真实 Codex 连接需要本机模型环境可用。
