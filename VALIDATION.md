# 提取版验证记录

日期：2026-09-17。测试解释器：Python 3.12（现有本地环境）。

| 验证 | 结果 |
|---|---|
| `python -m unittest discover -s tests -v` | 75 项测试全部通过，0 项跳过 |
| `python -m pac_harness --demo` | 一次本地列表替换、动作后检、完成声明复核；结果 `completed`，共 2 个规划轮 |
| 同一演示运行 `--resume` | 返回既有 `completed`，未重复执行动作 |
| 第二种动作/观察结构 | 内存文档发布测试通过；核心无需列表或空间字段 |
| 核心、角色规则、记忆和示例的场景依赖扫描 | 未发现原项目导入或原场景规则标识 |
| 顶层文档 | UTF-8、代码围栏及本地链接检查通过 |

测试覆盖：工具循环和预算、按需图像输入、未声明文件拒绝、动作格式、错误完成声明、结果丢失查询、后检与观察失败防重放、运行目录互斥、ToUser 指导及时保存、证据保护、并发修改检测、验证失败与部分应用回滚，以及提取的 Codex 连接、错误脱敏和热键行为。

模型请求、Codex 调用与沙箱验证命令在测试中使用替身；没有向真实模型发起请求、启动业务服务或操作外部设备。因此该记录验证的是软件契约与本地演示，不能作为真实模型规划能力、实际 Codex 沙箱可用性或新场景端到端效果的证明。

## 2026-09-20：ToUser 通用能力同步

将场景项目此前的 ToUser 改进适配到 Core，保留通用场景目录、网页输入及证据保护。运行 `python -X utf8 -m unittest discover -s tests`：**133 项全部通过，0 项跳过**。

- 四类意图及真实文件差异校验；临时指导不能冒充持久修改。
- 缺少 diff、语法错误、离线验证失败的有限自动修正；连接和并发错误不自动重写。
- Windows 输入时 Ctrl+G 返回、中文/Unicode 编辑、自然语言结束对话；网页输入不调用控制台读取。
- 指导累积、去重、即时保存，Ctrl+C 保留请求与对话；未绑定任务时不误报检查点保存。
- 知识更新后两 Agent 重新加载；代码更新保留 `post_pending`、BEFORE 和动作回执，恢复不重放。
- 自动修正的网页审计展示及场景隔离；并发修改、只读证据和回滚保护继续通过。

本轮使用离线模型/Codex 替身；未发起真实模型请求或运行设备。
