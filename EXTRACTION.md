# 通用提取记录

日期：2026-09-17。目标：从现有 `Harness-GPT` 中提取与具体场景无关的 Agent 运行机制，形成独立目录 `Harness-Core`。原项目保持原样，新项目不需要导入原项目或访问其邻接服务目录。

## 保留并泛化

| 原实现 | 新实现 | 提取方式 |
|---|---|---|
| `agent_runtime.py` | `pac_harness/runtime.py` | 保留 Planner→单动作→post、预算、主动用户介入和恢复语义，重写为 Environment 接口 |
| `agent_models.py` | `pac_harness/agents.py` | 保留多轮只读工具协议，动作与报告改为通用契约；配置模型供应商 |
| `agent_tools.py` | `pac_harness/tools.py` | 保留历史、状态、技能、观察和选证据能力；专业测量由适配器扩展 |
| `task_state.py`、日志组件 | `pac_harness/storage.py` | 保留发送前持久化、待后检、防重放；对象诊断改为通用 facts |
| `to_user.py` 的 Codex 后端 | `pac_harness/codex_backend.py` | 提取原 Codex 启动、会话续接、结构化返回、离线验证方法 |
| `codex_connection.py`、`codex_errors.py` | 同名通用模块 | 复用连接选择、模型设置及错误脱敏逻辑 |
| `console_hotkey.py` | 同名通用模块 | 复用 Windows Ctrl+G 监听，不包含业务自动对话 |
| `to_user.py` 的工作区流程 | `pac_harness/to_user.py` | 泛化隔离副本、证据、候选差异、验证、并发检查和回滚；指导即时写入检查点 |
| 角色记忆和技能 | `memories/`、`prompts/`、`skills/` | 仅保留通用角色原则，场景知识从空白模板开始 |

复用的连接、错误、热键测试保留离线测试方法，模块路径和模型/提供方测试样例已泛化。新增运行时、工具、多模态证据和 ToUser 应用测试。没有复制模型密钥、用户凭据或真实连接配置。

## 未提取的场景内容

原项目的设备接口、相机服务、坐标换算、标定、物体身份分区、夹取与插槽技能、对齐测量、固定动作集合，以及现场日志、图像、参考案例和维护历史均留在原项目。

新项目不硬编码具体设备、目标物体、观察视角、运动方向、距离或执行阶段。`examples/list_sorting.py` 是单独的演示适配器，不被核心导入，也不会成为新场景的默认业务知识。

## 与原项目的区别

- 独立起点版本为 `0.1.0`，不继承原应用的 2.x 场景版本号。
- 为彻底移除场景耦合，核心接口和检查点格式经过重写，不能直接恢复原应用的运行目录。
- 当前提供 Chat Completions JSON 工具协议；不携带原项目全部传输优化和专业视觉工具。
- ToUser 仍由本机 Codex CLI 支撑；沙箱验证参数保留现有 Windows 集成，其他平台需验证或替换后端。
- 修改成功后重新启动 worker，再按检查点继续，避免 Python 模块与新文件混用。
- 尚未提供跨 ToUser 会话的自动记忆检索、自动生成业务适配器或完整外部系统集成。

这次提取建立可复用的运行基础；不把原任务的成功率、软件测试数量或场景能力当作新项目的验证结果。
