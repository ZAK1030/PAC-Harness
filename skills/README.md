# Optional skills

Add a directory with a `SKILL.md` file for each reusable procedure. The core lists
the first 500 characters as a catalog entry and loads the full text on demand.
Scenario skills belong in the configured project's own skills directory. ToUser can
create one from `templates/SKILL.md`; it should only store repeatable procedures,
not one-off task instructions or unverified guesses.
